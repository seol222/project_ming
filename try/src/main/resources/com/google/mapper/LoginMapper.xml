<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.google.mapper.LoginMapper">
<select id="read" resultType="com.google.domain.MemberVO">
SELECT * FROM book_member WHERE mb_id=#{mb_id} AND mb_pw=#{mb_pw}
</select>
</mapper>